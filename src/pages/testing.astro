---
import Layout from "../layouts/Layout.astro";
import UnsplashCard from "../components/Card.astro";
import Search from "../components/Search.astro";
import ButtonFilter from "../components/ButtonFilter.astro";

// Definir el tipo de las cards
interface Card {
  id: number;
  title: string;
  subtitle: string;
  description: string;
  iconSrc: string;
  iconBg: string;
  cardBg: string;
}

// Cargar datos del archivo JSON
let cards: Card[] = [];

try {
  const response = await import("../data/Testing.json");
  const data = response.default || response;
  cards = data.cards || [];
} catch (error) {
  console.error("Error cargando JSON:", error);
  cards = [];
}
---

<Layout
  title="DevVault - Herramientas y frameworks para Testing"
>
  <div class="max-w-[1200px] mx-auto px-4 md:px-0">
    <!-- Header -->
    <header class="mb-10">
      <div class="mt-7">
        <h1 class="text-3xl md:text-4xl font-bold text-white mb-4">
          Testing Resources
        </h1>
        <p class="text-gray-400 mb-6">
          Frameworks, bibliotecas y herramientas para asegurar la calidad de tu código con testing automatizado.
        </p>
        <Search />
      </div>

      <div class="flex flex-wrap gap-2 mt-4">
        <ButtonFilter label="Unit Testing" />
        <ButtonFilter label="E2E Testing" />
        <ButtonFilter label="API Testing" />
        <ButtonFilter label="Automation" />
        <ButtonFilter label="Performance" />
      </div>
    </header>

    <!-- Recursos -->
    <section>
      <h2 class="text-2xl md:text-3xl font-semibold text-white mb-6">
        Recursos ({cards.length})
      </h2>

      <div
        class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-5"
      >
        {cards.map((card) => (
          <UnsplashCard
            title={card.title}
            subtitle={card.subtitle}
            description={card.description}
            iconSrc={card.iconSrc}
            iconBg={card.iconBg}
            cardBg={card.cardBg}
          />
        ))}
      </div>
    </section>
  </div>
</Layout>